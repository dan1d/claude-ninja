# Preferences & Feedback

#meta #memory

## Obsidian is the source of truth
Always read `PaydayBooks/Dev/Dev Tracker.md` before starting work. Update it when done.

## Agent routing
CLAUDE.md has an Agent Dispatch Protocol table. Route automatically. For multi-domain tasks use `/plan <task>` to fire `agent-organizer` first.
Primary agent for this stack: `rails-react-pro`.

## No Co-Authored-By lines in commits
Keep commit history clean — never add `Co-Authored-By: Claude...`

## Short messages = just do it
"next", "continue", "commit and push" — read Obsidian first, then act. No confirmation needed.

## No trailing summaries
One short sentence or paragraph max after completing a task. Daniel reads the diff and the tracker.